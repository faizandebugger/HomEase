class WorkerModel {
  String workerName;
  String workerImage;
  int workerAge;
  String workerAddress;
  int workerRating;
  String workerCategory;

  WorkerModel(this.workerName, this.workerImage, this.workerAge,
      this.workerRating, this.workerAddress, this.workerCategory);
}
